apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{.Release.Name }}-cast-pv
  labels:
    type: local
 #namespace: yaml
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{.Values.castDb.voulume.hostPath}}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{.Release.Name }}-cast-pvc
  #namespace: yaml
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
 name: {{.Release.Name }}-cast-db
 labels:
   app: {{.Release.Name }}-cast-db
 #namespace: yaml
spec:
  selector:
    matchLabels:
      app: {{.Release.Name }}-cast-db
  serviceName: {{.Release.Name }}-cast-db
  replicas: 1
  template:
   metadata:
     labels:
       app: {{.Release.Name }}-cast-db
   spec:
    volumes:
    - name: {{.Release.Name }}-cast-storage
      persistentVolumeClaim:
        claimName: {{.Release.Name }}-cast-pvc
    containers:
    - name: db-container
      image: {{Values.castDb.image.repository}}:{{Values.castDb.image.tag}}
   
      ports:
      - containerPort: {{.Values.castDb.service.port}}
        name: {{.Release.Name }}-cast-db
      volumeMounts:
        - mountPath: /var/lib/postgresql/data/
          name: {{.Release.Name }}-cast-storage
      env:
      - name: POSTGRES_USER
        value: "cast_db_username"
      - name: POSTGRES_PASSWORD
        value: "cast_db_password"
      - name: POSTGRES_DB
        value: "cast_db_dev"
      - name: POSTGRES_HOST_AUTH_METHOD
        value: "trust"
---
apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name }}-cast-db
  #namespace: yaml
spec:
  selector:
    app: {{.Release.Name }}-cast-db
  type: {{.Values.castDb.service.type}}
  clusterIP: {{.Values.castDb.service.type}}
  - name: {{.Release.Name }}-cast-db
    protocol: TCP
    port: {{.Values.castDb.service.port}}
    targetPort: {{.Values.castDb.service.port}}
